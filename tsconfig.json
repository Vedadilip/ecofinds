{
  "compilerOptions": {
    // --- Code Quality & Modern JavaScript Settings ---
    "target": "ES2020",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true,
    "noEmit": true,

    // --- Fixes for Your Environment ---

    // 1. Tells TypeScript how to understand JSX syntax (e.g., <div>). This is required.
    "jsx": "react-jsx",
    
    // 2. Explicitly tells TypeScript to ONLY use libraries available in a web browser.
    // This fixes the "Cannot find type definition file for 'node'" error.
    "lib": ["DOM", "DOM.Iterable", "ES2020"],
    
    // 3. Tells TypeScript to understand the modern "import/export" syntax you are using.
    "module": "ESNext",

    // 4. Informs TypeScript about the module resolution strategy.
    "moduleResolution": "bundler"
  },
  "include": [
    "./**/*.ts",
    "./**/*.tsx"
  ]
}
